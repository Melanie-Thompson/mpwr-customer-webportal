# Environment (local, dev, prod)
VITE_ENVIRONMENT=local

# API Configuration
VITE_API_URL=https://api.mpwr.com

# AWS Cognito Configuration (Customer/Borrower Pool)
# These are set via CircleCI contexts for dev/prod
# For local development, defaults to dev pool if not set
VITE_COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx
VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
VITE_COGNITO_DOMAIN=your-domain.auth.us-east-1.amazoncognito.com

# Copy this file to .env.local for local development
# CircleCI will create .env during build with values from context
